<%= form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


<%= f.text_field :name, maxlength: 15, style: "ime-mode: active;" %>


  <div class="field">
    <%= f.label :due_date %>日付<br>
    <%= f.date_select :due_date %>
  </div>
  <div class="field">
    <%= f.label :category_id %>カテゴリー<br>
    <%= f.collection_select :category_id,Category.all ,:categoryname,:categoryname %>
  </div>
  <div class="field">
    <%= f.label :name %>予定内容<br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :done %>チェック<br>
    <%= f.check_box :done %>
  </div>
  <div class="field">
    <%= f.label :user_id %>ユーザーID<br>
    <%= f.collection_select :user_id,User.all ,:username,:username %> 
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
